import { View, Text, SafeAreaView } from "react-native";
import React from "react";

const EatScreen = () => {
  return (
    <SafeAreaView>
      <Text>EatScreen</Text>
    </SafeAreaView>
  );
};

export default EatScreen;
